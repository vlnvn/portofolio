# Canonical public origin used by metadata, canonical URLs, robots and sitemap.
# Replace this value with the final HTTPS domain before production deployment.
NEXT_PUBLIC_SITE_URL=https://example.com
